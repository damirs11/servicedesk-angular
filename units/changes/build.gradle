/**
 * Раздел "Изменения"
 */
apply plugin: 'java'

repositories {
    mavenCentral()
    mavenLocal()
}

dependencies {
    compile project(':common')
    compile project(':units:changes-api')
}

test {
    useTestNG()
    systemProperty "file.encoding", "utf-8"
}