/**
 * Раздел "Изменения"
 */
apply plugin: 'java'

repositories {
    mavenCentral()
    mavenLocal()
}

dependencies {
    compile project(':common')
}

test {
    useTestNG()
    systemProperty "file.encoding", "utf-8"
}